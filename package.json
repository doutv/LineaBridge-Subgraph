{
  "name": "LineaBridge",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "auth": "graph auth --studio",
    "deploy": "graph deploy --studio lineabridge",
    "create-local": "graph create --node http://localhost:8020/ LineaBridge",
    "remove-local": "graph remove --node http://localhost:8020/ LineaBridge",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 LineaBridge",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.59.0",
    "@graphprotocol/graph-ts": "0.30.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.6.0"
  }
}
